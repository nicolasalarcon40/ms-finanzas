server:
  port: 8087

spring:
  application:
    name: ms-finanzas
  datasource:
    url: jdbc:mysql://localhost:3306/db_finanzas?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

ms:
  estudiantes:
    url: http://ms-estudiantes

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/